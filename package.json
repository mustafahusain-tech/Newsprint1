{
  "name": "newsprint-netlify",
  "version": "1.0.0",
  "description": "Daily curated news reader for the classroom — Netlify deployment",
  "type": "module",
  "engines": { "node": ">=18" },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.1",
    "@netlify/blobs": "^8.1.0"
  }
}
